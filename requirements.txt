pydantic>=2.6
pyyaml>=6.0
streamlit>=1.30
SQLAlchemy>=2.0,<3
# Dev tooling:
pytest>=8
pytest-asyncio>=0.23
ruff>=0.5
mypy>=1.10
types-PyYAML
# Live BLE mode (optional; pipeline still runs in --mock without it).
# Install separately when hardware is available:
#   pip install carreralib
